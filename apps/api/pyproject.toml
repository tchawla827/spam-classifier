[project]
name = "spam-classifier-api"
version = "0.1.0"
description = "Spam Classifier FastAPI backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 88
target-version = "py311"
